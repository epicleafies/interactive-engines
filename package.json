{
  "name": "interactive-engines",
  "version": "0.0.0",
  "private": true,
  "description": "Simulation engines and acceptance harnesses for a bitcoin education product.",
  "license": "UNLICENSED",
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "harness": "node --import tsx harness/run.ts",
    "selftest": "node --import tsx harness/self-test.ts",
    "engine-selftest": "node --import tsx harness/engine-selftest.ts",
    "test": "node --import tsx harness/self-test.ts && node --import tsx harness/engine-selftest.ts && node --import tsx harness/run.ts"
  },
  "devDependencies": {
    "@types/node": "^25.9.2",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2"
  }
}
